operatory: 
operator== 
bool operator==(const Kniha& druhy) const
operator+=
Roman& operator+=(int hodnota)
operator<<
friend ostream& operator<<(ostream& os, const Roman& druhy)
operator()
Cislo& Cislo::operator++()
operator+
Trida operator+(const Trida& druhy) const;

erase vektoru:
string.erase(string.begin()+i)
